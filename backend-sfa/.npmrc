# Configuration npm pour que tout reste sur le disque D
# Cache local du projet sur D:
cache=D:/SFA/.npm-cache

# Empêcher l'utilisation du cache global
prefer-offline=true

# Forcer les installations locales dans le projet
package-lock=false
